export enum InputType {
  TEXT = 'text',
  PASSWORD = 'password',
  EMAIL = 'email',
  CHECKBOX = 'checkbox',
  NUMBER = 'number',
}
export enum ValidatorType {
  required = 'required',
  pattern = 'pattern',
  maxlength = 'maxlength',
  minlength = 'minlength',
  max = 'max',
  min = 'min',
  requiredTrue = 'requiredTrue',
}
